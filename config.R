# config.R
# ========= 参数配置文件 ==========

# 时间范围
T <- 1            # 观察区间长度 [0, T]
X0 <- 1           # 初始值 log(X0) 起始点

# sigma(t) 模拟参数
a <- 1
alpha <- 1
k <- 4

# mu(t) 模拟参数
b <- 1.5
beta <- 1
l <- 4

# 主成分截断数
# L <- 2

# 仿真设置：样本数与观测数候选集合
K_grid <- c(500)       # 样本规模列表
n_ave_grid <- c(300)   # 每条曲线的观测数平均值列表

# 观测设置
m_default <- 50
n_ave_default <- 50
K_default <- 100
Nsim <- 10

# 估计核带宽最小阈值（用于二维协方差）
h_min <- 0.021
